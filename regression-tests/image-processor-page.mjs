import assert from 'node:assert/strict';
import fs from 'node:fs';

const page = fs.readFileSync('items/ImageProcessor.html', 'utf8');

assert.match(page, /data-tab="watermark"/);
assert.match(page, /data-tab="crop"/);
assert.match(page, /data-tab="compress"/);
assert.match(page, /data-tab="redact"/);
assert.match(page, /图片打码/);
assert.match(page, /id="rdClear"/);
assert.match(page, /id="rdExport"/);
assert.match(page, /id="rdToolRect"/);
assert.match(page, /id="rdToolBrush"/);
assert.match(page, /data-effect="mosaic"/);
assert.match(page, /data-effect="blur"/);
assert.match(page, /data-effect="solid"/);
assert.match(page, /data-effect="emoji"/);
assert.match(page, /🫣/);
assert.match(page, /🙈/);
assert.match(page, /😀/);
assert.match(page, /⭐/);
assert.match(page, /🚫/);
assert.match(page, /💦/);
assert.match(page, /💩/);
assert.match(page, /❤️/);
assert.match(page, /🔥/);
assert.match(page, /✅/);
assert.match(page, /clearImages\('redact'\)/);
assert.match(page, /bindDrop\('rdDrop', 'rdFile', 'redact'\)/);
assert.match(page, /tab === 'redact'/);
assert.match(page, /cropperjs\/1\.6\.2\/cropper\.min\.css/);
assert.match(page, /cropperjs\/1\.6\.2\/cropper\.min\.js/);
assert.match(page, /jszip\/3\.10\.1\/jszip\.min\.js/);
assert.match(page, /image-processor-core\.js/);
assert.match(page, /theme\.js/);
assert.match(page, /17tools-image-processor/);
assert.equal(page.includes('innerHTML = file.name'), false);
assert.match(page, /\[hidden\]\s*\{\s*display:\s*none\s*!important/);
assert.match(page, /class="form-input"/);
assert.equal(page.includes('#wmCanvas, #cpPreview, #cropImage { max-width: 100%; max-height: 420px; display: block'), false);
assert.match(page, /id="wmBatch"/);
assert.match(page, /id="cpBatch"/);
assert.match(page, /id="wmClear"/);
assert.match(page, /id="cropClear"/);
assert.match(page, /id="cpClear"/);
assert.match(page, /id="wmStyle"/);
assert.match(page, /option value="stroke"/);
assert.match(page, /option value="shadow"/);
assert.match(page, /option value="stamp"/);
assert.match(page, /option value="banner"/);
assert.match(page, /id="wmFontFamily"/);
assert.match(page, /id="wmBold"/);
assert.match(page, /function fitCropperCanvas/);
assert.match(page, /clearImages\('watermark'\)/);
assert.match(page, /clearImages\('crop'\)/);
assert.match(page, /clearImages\('compress'\)/);
assert.match(page, /syncBatchInput\('watermark'\)/);
assert.match(page, /syncBatchInput\('compress'\)/);
assert.match(page, /\(tab === 'crop' \|\| tab === 'redact'\) && state\[tab\]\.items\.length/);
assert.match(page, /bindDrop\('wmDrop', 'wmFile', 'watermark'\);/);
assert.match(page, /bindDrop\('cropDrop', 'cropFile', 'crop'\);/);
assert.match(page, /bindDrop\('cpDrop', 'cpFile', 'compress'\);/);
assert.equal(/id="wmFile"[^>]*\bmultiple\b/.test(page), false);
assert.equal(/id="cpFile"[^>]*\bmultiple\b/.test(page), false);
assert.equal(/id="rdFile"[^>]*\bmultiple\b/.test(page), false);
assert.equal(page.includes('id="rdBatch"'), false);
assert.match(page, /function readRedactSettings/);
assert.match(page, /function exportRedact/);
assert.match(page, /function compositeRedact/);
assert.match(page, /originRegions: cloneRegions/);
assert.match(page, /mosaicSize: 16, blurStrength: 8, solidColor: '#000000'/);
const saveFn = page.match(/function saveSettings\(\) \{[\s\S]*?\n        \}/)[0];
assert.match(saveFn, /redact: \{/);
assert.equal(/effect:/.test(saveFn), false);
assert.equal(/tool:/.test(saveFn), false);
assert.equal(/regions:/.test(saveFn), false);
const ingestFn = page.match(/async function ingestFiles[\s\S]*?\n        function selected/)[0];
assert.match(ingestFn, /state\.redact\.drag = null/);
assert.match(page, /#cropDrop\.has-image/);
assert.match(page, /viewMode:\s*2/);
assert.match(page, /fitCropperCanvas\(\)/);

const index = fs.readFileSync('index.html', 'utf8');
const welcome = fs.readFileSync('items/Welcome.html', 'utf8');
const history = fs.readFileSync('items/History.html', 'utf8');
const version = fs.readFileSync('js/version.js', 'utf8');
const readme = fs.readFileSync('README.md', 'utf8');

assert.match(index, /data-file="ImageProcessor.html"/);
assert.match(index, /图片处理/);

assert.match(welcome, /19个实用工具/);
assert.match(welcome, /v2\.16\.4/);
assert.match(history, /<!-- 版本 2\.16\.4 -->/);
assert.match(history, /版本 2\.16\.4/);
assert.match(history, /2026-08-19/);
assert.match(history, /图片打码/);
assert.match(history, /<!-- 版本 2\.15\.2 -->/);
assert.match(history, /版本 2\.15\.2/);
assert.match(history, /<!-- 版本 2\.15\.1 -->/);
assert.match(history, /版本 2\.15\.1/);
assert.match(history, /<!-- 版本 2\.15\.0 -->/);
assert.match(history, /版本 2\.15\.0/);
assert.match(history, /2026-08-13/);
assert.match(history, /图片处理：新增独立工具页/);
assert.match(history, /智能白板：跟随全局黑暗模式/);
assert.match(history, /智能白板：导出 PNG 时可选择浅色或深色合成/);
assert.match(version, /2\.16\.4/);
assert.equal(version.includes('2.16.3'), false);
assert.equal(version.includes('2.16.2'), false);
assert.equal(version.includes('2.16.1'), false);
assert.equal(version.includes('2.15.2'), false);
assert.equal(version.includes('2.15.1'), false);
assert.equal(version.includes('2.15.0'), false);
assert.equal(version.includes('2.14.1'), false);
assert.match(readme, /图片处理/);
assert.match(readme, /ImageProcessor\.html/);
assert.match(readme, /image-processor-core\.js/);

console.log('image-processor-page regression passed');
